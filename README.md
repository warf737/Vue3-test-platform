# Vue 3 Test Platform

Платформа для управления тестовыми проектами и заданиями, построенная на Vue 3 с использованием современного стека технологий.

## 📋 Описание

Проект представляет собой веб-приложение для управления решениями и заданиями. Пользователи могут просматривать список решений и заданий.

## 🛠 Технологический стек

- **Vue 3** (Composition API) - основной фреймворк
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Vue Router** - маршрутизация
- **Pinia** - управление состоянием
- **Element Plus** - UI компоненты
- **ESLint** - линтинг кода
- **Prettier** - форматирование кода
- **Typograf** - автоматическая типографика текста

## 📁 Структура проекта

```
saasoft-vue3-test-form/
├── src/
│   ├── assets/              # Статические ресурсы (стили, иконки, изображения)
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Card/           # Компонент карточки
│   │   ├── Divider/        # Компонент разделителя
│   │   └── DynamicForm/    # Динамическая форма
│   ├── composables/        # Композиционные функции (хуки)
│   │   └── useComputedStyle.ts
│   ├── interfaces/         # TypeScript интерфейсы
│   │   ├── accounts.ts
│   │   ├── formFields.ts
│   │   ├── solutions.ts
│   │   └── tasks.ts
│   ├── router/             # Конфигурация роутера
│   │   └── index.ts
│   ├── stores/             # Pinia хранилища (если есть глобальные)
│   ├── utils/              # Утилиты
│   │   ├── styleUtils.ts
│   │   └── typography.ts
│   ├── views/              # Страницы приложения
│   │   ├── About/          # Страница "О проекте"
│   │   ├── Solutions/     # Страницы решений
│   │   │   ├── solutions/
│   │   │   │   └── UsersList/  # Модуль управления учетными записями
│   │   │   │       ├── components/      # Компоненты модуля
│   │   │   │       ├── composables/      # Хуки модуля
│   │   │   │       │   ├── useAccountForms.ts
│   │   │   │       │   ├── useAccountsManagement.ts
│   │   │   │       │   └── useModuleRouter.ts
│   │   │   │       ├── constants.ts      # Константы модуля
│   │   │   │       ├── router/          # Роуты модуля
│   │   │   │       ├── store/           # Хранилище модуля
│   │   │   │       │   ├── accounts.ts
│   │   │   │       │   └── index.ts
│   │   │   │       └── views/           # Представления модуля
│   │   │   │           └── UserListView.vue
│   │   │   ├── SolutionDetailView.vue
│   │   │   ├── SolutionsView.vue
│   │   │   └── constants.ts
│   │   └── Tasks/          # Страницы заданий
│   │       ├── TaskDetailView.vue
│   │       ├── TasksView.vue
│   │       └── constants.ts
│   ├── App.vue             # Корневой компонент
│   └── main.ts             # Точка входа приложения
├── .eslintignore           # Исключения для ESLint
├── .eslintrc.cjs           # Конфигурация ESLint
├── .prettierrc              # Конфигурация Prettier
├── index.html               # HTML шаблон
├── package.json             # Зависимости и скрипты
├── tsconfig.json             # Конфигурация TypeScript
├── tsconfig.app.json         # Конфигурация TypeScript для приложения
├── tsconfig.node.json        # Конфигурация TypeScript для Node.js
└── vite.config.ts            # Конфигурация Vite
```

## 🚀 Установка и запуск

### Требования

- **Node.js**: версия `^20.19.0` или `>=22.12.0`
- **npm**: версия 9.x или выше

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:4000`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`.

### Предпросмотр production сборки

```bash
npm run preview
```

## 📜 Доступные скрипты

### Разработка

- **`npm run dev`** - Запуск dev-сервера с hot module replacement (HMR)
- **`npm run preview`** - Предпросмотр production сборки локально

### Сборка

- **`npm run build`** - Полная сборка проекта (type-check + build)
- **`npm run build-only`** - Только сборка без проверки типов
- **`npm run type-check`** - Проверка типов TypeScript

### Линтинг и форматирование

- **`npm run lint`** - Запуск ESLint с автоматическим исправлением ошибок
- **`npm run lint:check`** - Проверка кода без исправлений
- **`npm run format`** - Форматирование кода с помощью Prettier
- **`npm run format:check`** - Проверка форматирования без изменений

## 🏗 Архитектура

### Модульная структура

Проект использует модульную архитектуру, где каждый модуль решения (например, `UsersList`) имеет свою структуру:

- **`composables/`** - переиспользуемая логика (хуки)
- **`store/`** - управление состоянием модуля
- **`router/`** - маршруты модуля
- **`views/`** - компоненты представления
- **`components/`** - компоненты модуля
- **`constants.ts`** - константы модуля

### Управление состоянием

- **Pinia** используется для глобального и модульного состояния
- Каждый модуль может иметь свой собственный стор
- Данные сохраняются в `localStorage` для персистентности

### Роутинг

- Основные маршруты настраиваются в `src/router/index.ts`
- Модули могут определять свои вложенные маршруты
- Используется динамическая загрузка компонентов

### Композиционные функции (Composables)

Проект активно использует composables для переиспользования логики:

- **`useAccountForms`** - управление формами учетных записей
- **`useAccountsManagement`** - управление учетными записями
- **`useModuleRouter`** - работа с роутингом модулей
- **`useComputedStyle`** - вычисление стилей

## 🎯 Основные возможности

- ✅ Просмотр списка решений и заданий
- ✅ Управление учетными записями с валидацией
- ✅ Автоматическое сохранение данных в localStorage
- ✅ Валидация форм с отображением ошибок
- ✅ Поддержка различных типов учетных записей (LDAP, локальные)
- ✅ Динамическое управление метками (labels)
- ✅ Hot Module Replacement (HMR) для быстрой разработки

## 🔧 Конфигурация

### Vite

Конфигурация находится в `vite.config.ts`:
- Порт dev-сервера: `4000`
- HMR включен с overlay для ошибок
- Алиас `@` указывает на `src/`

### TypeScript

- `tsconfig.json` - основная конфигурация
- `tsconfig.app.json` - для приложения
- `tsconfig.node.json` - для Node.js окружения

### ESLint и Prettier

- Конфигурация ESLint: `.eslintrc.cjs`
- Конфигурация Prettier: `.prettierrc`
- Исключения: `.eslintignore`

## 📝 Рекомендации по разработке

### IDE Setup

Рекомендуется использовать:
- **VS Code** с расширением [Vue (Official)](https://marketplace.visualstudio.com/items?itemName=Vue.volar)
- Отключить расширение Vetur, если оно установлено

### Браузерные расширения

Для отладки рекомендуется установить:
- **Vue.js devtools** для Chrome/Firefox
- Включить Custom Object Formatter в DevTools

## 🤝 Вклад в проект

При работе с проектом следуйте установленным правилам:
1. Запускайте `npm run lint` перед коммитом
2. Используйте `npm run format` для форматирования кода
3. Проверяйте типы с помощью `npm run type-check`

## 📄 Лицензия

Проект создан для тестирования и обучения.
